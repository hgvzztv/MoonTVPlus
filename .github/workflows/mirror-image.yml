name: Mirror Docker image to GHCR
on:
  workflow_dispatch: {}
permissions:
  contents: read
  packages: write
jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Pull source image
        run: docker pull ghcr.io/mtvpls/moontvplus:latest
      - name: Re-tag
        run: |
          docker tag ghcr.io/mtvpls/moontvplus:latest ghcr.io/${{ github.actor }}/hf-mtv-dma:latest
      - name: Push to GHCR
        run: docker push ghcr.io/${{ github.actor }}/hf-mtv-dma:latest
      - name: Show result
        run: |
          echo "Pushed: ghcr.io/${{ github.actor }}/hf-mtv-dma:latest"
